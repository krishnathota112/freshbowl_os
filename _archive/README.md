# `_archive/` — not source code

Material that used to sit at the repository root. Kept, not deleted; moved here on 14 Sep 2026.

| Item | What it is |
|---|---|
| `MushroomOS-extracted/` | An unpacked earlier MushroomOS APK. `assets/public/` is the hand-built UI the factory already had — a visual reference. |
| `MushroomOS-extracted.zip` | The archive that folder came from |
| `apk_extracted/` | Another unpacked APK build (reference only — never copy a build into it) |
| `MushroomOS-debug.apk` | An old debug APK (13 Sep). The current APK is built to `mushroomos/android/app/build/outputs/apk/debug/app-debug.apk` |
| `memory.zip` | A zipped notes/memory folder |
| `Freshbowl Mushroom Composting Workflow Automation.docx` | The original workflow-automation brief |

Older documents may still refer to `MushroomOS-extracted/` or `apk_extracted/` at the root; they now
live here.
