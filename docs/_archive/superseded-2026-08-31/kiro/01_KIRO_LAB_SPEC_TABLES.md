> **SUPERSEDED 31 Aug 2026 — do not build from this file.**
>
> It specified `lab_checkpoint` as a new text-keyed table. That name is already taken by a
> uuid-keyed execution table with live foreign keys, and the same brief forbade altering existing
> tables — two instructions that cannot both be satisfied. The implementation agent stopped, which
> was correct.
>
> **Current instruction: `docs/03-mission/kiro/01_RULING_LAB_SPEC_MODEL.md`.**
>
> Read that instead. It rules on the collision, keeps the existing RPC names, leaves working gates
> alone, and makes the first deliverable a schema comparison rather than a migration.
